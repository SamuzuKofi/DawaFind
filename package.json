{
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "firebase": "^12.16.0",
    "jest": "^30.4.2"
  },
  "scripts": {
    "test:rules": "firebase emulators:exec --only firestore \"npx jest firestore-rules.test.js\""
  }
}
